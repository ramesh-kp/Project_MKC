username	:	pimkc
password	:	Naico@1234

To set up the ethernet port on a Raspberry Pi, follow these instructions.
	sudo nano /etc/dhcpcd.conf
		#interface eth0
		#static ip_address=192.168.1.75
		#static routers=192.168.0.3
		#static domain_name_servers=8.8.8.8.8.8.4.4

Use the command to install a library on a Raspberry Pi.
	python3 -m pip install <<< whatever >>>

